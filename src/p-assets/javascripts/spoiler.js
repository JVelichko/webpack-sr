/* console.log('hi');
var spoiler = document.querySelector('.spoiler-trigger'); */

//console.log(modal);
/* spoiler.addEventListener('click', handleClick);

function handleClick(e) {
  console.log(e.target);
  console.log(this.parentElement.nextElementSibling.collapse('toggle'));
}
 */

//import * as $ from jQuery
/* var $ = require('jQuery');
console.log($(".spoiler-trigger"));
$(".spoiler-trigger").click(function() {
    console.log('kek');
    $(this).parent().next().collapse('toggle');
}); */
 
/* 
  $(document).on('click', '.panel div.clickable', function (e) {
    var $this = $(this); //Heading
    var $panel = $this.parent('.panel');
    var $panel_body = $panel.children('.panel-body');
    var $display = $panel_body.css('display');

    if ($display == 'block') {
        $panel_body.slideUp();
    } else if($display == 'none') {
        $panel_body.slideDown();
    }
});

$(document).ready(function(e){
    var $classy = '.panel.autocollapse';

    var $found = $($classy);
    $dfound.find('.panel-body').hide();
    $found.removeClass($classy);
});
 */
